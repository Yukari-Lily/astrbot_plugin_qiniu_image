# 2026-09-17 运行日志对照

依据用户提供的本地日志片段；原始日志不纳入仓库。阶段根据提交日志的字段及内容区分，不将时间段本身当作版本证明。

| 观察项 | 更新前 09:15–09:18 | 更新后 10:07 |
| --- | --- | --- |
| 请求样本 | 4 次：画千束、再画，来自两个 Bot 实例 | 2 次：画千束，来自两个 Bot 实例 |
| 优化后正文长度 | 4、20、20、4 字符，基本只有角色名和作品名 | 280、306 字符，包含身份、服装、姿态、构图与光影 |
| 风格 | 错位窗口两次、米白红色电路两次 | 净色动画壁纸两次，都是单人 |
| 提交方式 | `style_appended=True` | `integrated=True` |
| 优化 Provider | 两阶段相同 | 两阶段相同 |
| 图像参数可见性 | 此片段未记录实际模型、质量和尺寸 | `openai/gpt-image-2`，quality/size 均为 auto |
| 发送证据 | 4 次成功发送日志 | 片段停在两次提交，没有最终发送日志 |

## 已确认的改善

优化器已输出完整视觉描述，提交方式已经切换为融合稿；修复并非只改了文档或未生效。提示词长度仅说明内容发生变化，不能作为画质评分。

用户另外确认更新后的两次请求均正常收到图片，效果有改善。该结论来自用户成图反馈，不是由缺失的发送日志推断出来；当前片段不足以计算更新后的生成耗时或比较成功率。

## 发现的问题与本次处理

- 两次更新后单人请求都选净色，证实恢复 1.3 默认偏好后产生了不希望的风格倾向。本次将净色降到目录末尾，仅允许有输入依据的至少两名人物；增加人数与引用来源校验，并测试错误选择会先重试、不会提交图片服务。
- 更新后主模型仍只传“千束”或“锦木千束，Lycoris Recoil”，完整视觉描述主要由优化器补出。因此日志尚不能证明“主模型先完成设计”的分工已落实。更新后需重新加载插件刷新工具说明，并在新会话中核对工具参数；不能仅凭此日志认定工具缓存就是原因。
- 两份优化稿对发带左右侧的描述不一致。日志可以证明描述冲突，不能证明哪份符合角色设定，也不能据此量化实际人体畸形率。

## 验证范围

本次净色限制通过离线回归测试；这份日志采集于限制实施前，不能冒充限制实施后的线上验证。关键词直出保持原行为。后续对照建议固定模型、质量、尺寸和请求，各生成至少三张，同时保存主模型工具参数、融合执行稿及成图。
