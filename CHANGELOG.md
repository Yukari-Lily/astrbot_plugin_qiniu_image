# 更新日志

## 2.0.0

- 从 1.3.0 基线重构，删除 1.4.x 的全部新增：结构化任务、编辑继承、图片用途绑定、身份状态枚举、磁盘作品存储、出图后视觉核对，共减少约 930 行。
- 修复冷门角色画不准的根本原因：外观此前以自由字符串交给优化模型转述，而链路上没有任何一环被要求保留它，抓图与核对换来的信息在最后一跳蒸发。
- 外观改为**定键记录**（剪影、发型、发长、发色、前发、瞳色、眼型、服装、剪裁、肤色、体型等），取代无 schema 的 `features` 自由字符串列表，字段顺序即渲染顺序，按身份承载能力排序。
- 外观由插件**确定性渲染**并逐字写入最终提示词，紧贴所属人物标题行下方；优化模型不再接收外观，也无从丢弃它。`render_block` 是最终提示词的字面子串，由离线测试断言。
- 覆盖度校验把"模型偷懒"变成确定性失败，并按严重度分档：**必需字段**缺失、占位、过短、含未定表述，或同一描述铺满 4 个以上字段，属致命问题，整份记录不可用；**可选字段**上的同类瑕疵只让那一项不渲染，不连累记录。致命时才带问题清单定向重试一次，仍不合格则返回 `unavailable` 且不猜、不出图。
- 作图约定补充自足性要求：写进 `prompt` 的方案必须自足，图片模型看不到这段对话，不能出现"刚才那张""她"这类指代——这是主模型侧唯一防不住的上下文污染，优化模型上下文更少，也替不了它。
- 抽取环节把身份依据（`identity_basis`）与外观彻底分开记录，不再混进同一个 `summary` 字段。
- 网页参考图**默认不进入绘图接口**，只用来产出文字；只有用户原消息自带的 `input:N` 图片才作为图片输入传给七牛。
- 参考工具改为只收人物名/作品/版本与模型搜索到的来源网址：搜索沿用 AstrBot 已有的联网能力，插件不新增搜索密钥。下载、选图、核对、抽取全在插件内。
- 新增外观缓存：按 `作品|名字|版本` 的内容键索引，另存准确名别名键，TTL 6 小时 / 最多 200 条，纯文本、进程内、不落盘。
- 五级安全回退重新拼装时只回注**身份内核**，不再回注服装，避免合法软化掉的暴露服装被原样塞回去反复被拒。
- 注入系统提示词的作图约定由约 1750 字精简到约 400 字，消歧规则移入工具描述。
- `style_presets.py` 未改动：13 个预设、`style_mode`、`style_strength`、风格标记协议、目录工具与自动偏好规则全部保留，风格照常应用。
- 提高内置风格的套用频率：风格路由启用时随路由一同下发"不要输出 `STYLE_PRESET:none`"的约束（用户明确要求其他画风、纯写实、复刻原画风、不要风格化时除外）；优化模型仍然选了 `none` 时补一次定向重试，重试仍选 `none` 则接受，不阻断出图。
- 离线测试重写为 49 项，覆盖渲染顺序、覆盖度拒绝、锁不变量、优化器丢弃、安全回退、缓存与别名键、定向重试、风格套用频率、网页结果地址解析与 SSRF 限制。
- 顺带修掉四处实现缺陷：外观记录规范化会静默丢弃嵌套数组（`outfit.pieces`）；候选图的来源页地址被图片地址覆盖导致页面正文与图片错配；短值字段（发色"黑"、配色"金"）被过短规则误判为不合格，会让整个角色退回纯文字出图；没有人物名单时把普通提示词文本当 JSON 解析，导致每一次"画一张某物"都判成优化失败。
- 修正一处从未真正执行的测试：DNS 重绑定防护用例写在 `unittest.TestCase` 里的 `async def`，unittest 只会创建协程再丢弃。改为 `asyncio.run` 驱动后确认防护本身有效。

## 1.4.1

- 调整人物消歧：沿用会话与目标作品已确认指代，区分身份确认和外观核准；搜索无关时放宽类别并核实已有别名线索，避免将真人昵称锁在动漫游戏类别。
- 近期作品摘要加入身份依据，按预算缩短可选详情，保留完整作品标识与 JSON。
- 自动画风在主聊天模型和优化器两处优先采用相容的内置高偏好项，区分用户指定与模型自选媒介；保留外部指定风格、编辑原貌和风格模式配置。
- 风格路由校验单一有效选择标记，缺失时使用已有优化重试与回退，不追加绘图。
- 减少错位、波普、电路与思维爆发预设的装饰线和碎片，绘图接口直接接收线条约束，保留人物标志与明确艺术要求。
- 补充离线回归验证；实际主模型消歧效果及成图清洁度仍需更新后实测。

## 1.4.0

- 新增结构化绘图任务，区分新画、局部编辑、整张重画，以及人物、风格和编辑原图三种图片用途。
- 分开记录用户明确要求、保留项、人物事实和模型创作选择；多人提示词逐人输出并校验人物顺序和数量。
- 聊天请求前注入有界风格词表与当前用户近期作品摘要，并提供完整嵌套工具参数 schema。
- 作品按会话与用户隔离，保存真实提示词、人物资料、参考图和成图；支持按作品标识直接继承，后台启动前固定基础记录。
- 新增网页参考图工具，复用内置 Tavily 的结果地址，通过现有优化模型核对候选图，再将逐人参考图交给优化与绘图模型。
- 多人出图后执行一轮视觉检查，发现问题时提示，不自动额外出图；不增加补图追问或等待任务。
- 临时图片与附件缓存统一限制为 512 MiB / 100 条 / 30 分钟，支持自动过期清理和卸载清理。
- 风格列表仅输出 13 个中文标题。旧 `draw_image(prompt)` 和直接触发词继续兼容。
- 增加离线任务继承、图片绑定、API 请求、视觉回退与网页读取测试。

## 1.3.0

- 删除优化模型查看原图的配置与代码路径；原图仅交给最终图片模型。
- 配置项由 27 项精简为 10 项；保留风格模式、风格强度与关键词直出，其余稳定策略改为插件内置常量。
- 默认图像模型改为 `openai/gpt-image-2.5-sunburst`，并为模型、画质和尺寸提供明确选项。
- 默认并发数提高到 3；图片请求超时提高到 480 秒，并增加连接超时、重试次数和 4K 图片体积冗余。
- 固定使用官方 API、后台出图、PNG 输出和五级安全回退。
- 按 Modelink 文档将单张输入图限制为 40 MB，补充 GIF 输入支持；输出接收上限独立设为 80 MiB。
- 将有效提示词长度上限对齐至 Modelink 的 32000 字符。

## 1.2.0

- 恢复主聊天模型对绘图意图的完整理解职责：结合 Bot 人设、群聊上下文和用户委托形成完整绘图方案，再交给提示词优化模型整理。
- 收紧提示词优化模型的职责：除配置明确启用的全局质量规则与内置风格外，不得自行增加或替换画面创作内容。
- 优化模型不再同时接收用户原始短句，避免它绕过主聊天模型重新解释“自拍”等上下文意图。
- 允许主聊天模型通过串行搜索补全陌生或易混淆的动画角色资料，并保留相容的考据结果与视觉方案。
- 移除固定轮数历史注入，避免优化模型用残缺群聊片段猜测指代。
- 新增 `get_last_image_prompt` 工具，按会话读取最近一次真正提交给图片模型的提示词，支持透明、准确地延续和修改上一张图。
- 加强动画画面的全局清洁度控制：大色块、低视觉频率、受控边缘密度、少量内部轮廓线、大块阴影，并抑制微纹理、碎高光与密集装饰。
- 新增高自动偏好的“净色动画壁纸”风格。
- 安全改写模型在某一级拒绝或未返回有效提示词时，继续推进到下一安全级别，而非提前终止整个请求。

## 1.1.0

- 统一函数工具与关键词直出的提示词语义：用户原话作为唯一创作需求，工具草稿仅解析指代和主体身份。
- 收紧 `draw_image` 调用说明：常见角色直接绘制，仅在无法唯一识别时先完成 Web 搜索，禁止搜索与绘图并行。
- 改写时区分用户原话与聊天模型生成的工具参数，避免泛化质量词阻止内置风格生效。
- 新增默认审美兜底，角色题材优先保持清爽、高级的二次元表现；仅在未使用内置风格时注入。
- 新增 `list_image_styles` 工具，让聊天模型可查询实际内置风格。
- 支持单独指定提示词改写 Provider。
- 支持提示词改写 Provider 重试与有序备用 Provider，全部失败时不再发送未经优化的原提示词。
- 主改写模型与备用模型合并为 AstrBot 有序模型选择器，无需手填 Provider ID。
- 重排并精简配置界面文案，常用选项优先显示，枚举项保留真实英文值。
- 内置风格名改为仅供路由与日志使用，出图前自动清除，避免被画进海报。
- 全局审美防脏规则恢复为所有风格必定注入，并支持在配置中自定义。
- 日系 IP 默认使用官方日文作品名、角色名和画内字样。
- 图片安全审核拒绝后，可多次生成合规替代提示词并重试。
- 首次出图不做风险词检测或安全转译，仅在图片平台实际拒绝后启用安全改写。
- 安全拒绝后的默认改写重试增至 5 次，并按五级策略逐次提高安全程度：先以同义替换、隐晦措辞尝试保留原画面，仍被拒绝后才逐步收敛画面。
- 安全转译前两级加入二次元插画语境框架声明与风险元素叠加规避规则。
- 新增 `moderation` 配置（默认 low，放宽审核阈值；可设 auto 恢复上游默认）。
- 历史对话注入默认关闭，避免群聊上下文串扰。

## 1.0.0

- 增加内置风格库及自动风格路由。
- 增加后台出图、历史对话注入与视觉改写配置。
